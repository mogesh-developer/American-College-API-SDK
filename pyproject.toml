[project]
name = "amc-api"
version = "0.1.0"
description = "Python SDK for American College Student Portal"
authors = [
    {name="Mogeshwaran M"}
]

dependencies = [
    "requests",
    "python-dotenv"
]

requires-python=">=3.10"
